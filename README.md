# 后室 · THE BACKROOMS

一个可直接通过 GitHub Pages 托管的静态 Three.js 生存解谜游戏。

## 入口

打开 `index.html` 即可进入游戏。部署到 GitHub Pages 后，访问仓库的 Pages 链接即可游玩。

## 资源

游戏运行依赖 `assets/` 目录内的模型、贴图和音频资源，以及 `index.html` 中通过 import map 加载的 Three.js CDN。

`assets/source_models/` 保留下载目录中的原始/备选模型素材，便于后续继续替换、调参或重新接入场景。
